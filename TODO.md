>> Install react router

>> Define routes in routes in js for multiple screens

>> Create separate screens in screens dir for each screen

>> Modifer app.js to display router

>> Start Main Menu to display functionality, add styles if needed

>> May need custom hooks (later) (Context hook with state hook for game state management)

>> JSON format for card data: Card_Name, Tags, Image - store in js/xml.

>> Rename, resize and trim existing images for demo images.